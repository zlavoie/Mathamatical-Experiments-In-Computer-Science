         !Zoe Lavoie
         write (*,*) 'You have to learn to pace yourself... FORTRAN'  !Have to use double '' to print out one '
         write (*,*)  'You''re just like everybody else...FORTRAN'
         write (*,*) 'You''ve only had to write C++ so far'
         write (*,*) 'But you will come to the day'
         write (*,*) 'When the only thing that counts'
         write (*,*) 'Are megaflops on a Cray'
         write (*,*) 'And you''ll have to deal with... FORTRAN'
        pause
         end
